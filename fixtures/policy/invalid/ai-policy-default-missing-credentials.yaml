# Invalid: the 'default' profile requires all four blocks from the spec
# section 20 example; 'credentials' is missing.
ai:
  agents:
    default:
      filesystem:
        workspace: read_write
      host:
        user_package: allow
      network:
        internet: allow
