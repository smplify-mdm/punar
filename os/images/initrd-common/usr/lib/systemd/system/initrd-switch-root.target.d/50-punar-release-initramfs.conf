[Unit]
# Free the unpacked initramfs as the last step before switch-root. The kernel
# keeps it resident for the whole boot on Linux 7.0 to 7.2
# (PERFORMANCE_BUDGETS.md, "Unpacked initramfs").
Wants=punar-release-initramfs.service
