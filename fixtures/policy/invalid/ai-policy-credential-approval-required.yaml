# Invalid: credential grants use the manifest-local enum allow|deny|request
# (spec sections 17, 20); 'approval_required' belongs to the shared runtime
# decision enum and is not a valid credential value.
ai:
  agents:
    default:
      filesystem:
        workspace: read_write
      host:
        user_package: allow
      network:
        internet: allow
      credentials:
        aws_dev: approval_required
