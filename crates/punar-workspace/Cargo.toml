[package]
name = "punar-workspace"
description = "Project workspace and window-context manager (SPEC section 11.5) — Milestone 2 ships the typed workspace-state contract; no daemon runs yet"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
