[Service]
# Keep the kernel console on the local display, but make a failed switch-root
# command explain itself on the dedicated CI proof UART. Successful boots are
# silent here and are attested separately by punar-installer-ready.service.
StandardOutput=tty
StandardError=tty
TTYPath=/dev/ttyS0
