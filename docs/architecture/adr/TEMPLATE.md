# ADR-NNN: Title

- Status: In progress | Accepted | Superseded by ADR-NNN | Rejected
- Date: YYYY-MM-DD
- Spec references: sections of `docs/product/SPEC_v0.2.md` this decision serves

## Context

What forces are at play. The problem being decided, the constraints (spec
requirements, performance budgets, maintainer environment, upstream realities),
and why the decision is needed now. State facts, not preferences.

## Options considered

One subsection per option. For each: what it is, how it satisfies or fails the
constraints above, and its costs. Include the option of doing nothing when it
is real. Comparisons should cite evidence (measurements, upstream docs,
prototypes) and label anything unverified or simulated as such.

### Option A — name

### Option B — name

## Decision

The choice, stated in one or two sentences, and the deciding factors that
outweighed the alternatives. If the decision is provisional or scoped (e.g.
"for the MVP only"), say so explicitly.

## Consequences

What becomes easier, what becomes harder, and what work the decision commits
us to. Include negative consequences honestly — they are the part future
readers need most.

## Revisit triggers

Concrete conditions under which this decision must be re-examined (e.g. a
budget is missed by a stated margin, an upstream project changes course, a
milestone reveals a blocking limitation). A trigger firing means opening a new
ADR, not silently editing this one.
