# Punar release greeter. No initial_session is allowed: onboarding creates
# the first human account, then normal authentication starts the compositor.

[terminal]
vt = 1

[default_session]
command = "/usr/lib/punar/greeter-session.sh"
user = "greeter"
